require 'spec_helper'

describe ScoutStatsdRack do
  it 'has a version number' do
    expect(ScoutStatsdRack::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
